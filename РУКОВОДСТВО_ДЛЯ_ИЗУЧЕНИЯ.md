# Руководство по изучению кода проекта AlR1_Beats

## 📁 Структура проекта

```
Alone_project/
├── Site.html          # Главная страница
├── about.html         # Страница "О нас"
├── service.html       # Страница "Услуги"
├── release.html       # Страница "Выпуск музыки"
├── provile.html       # Страница "Профиль" (опечатка: должно быть profile)
├── login.html         # Страница "Вход/Регистрация"
├── contact.html       # Страница "Контакты"
├── css/
│   └── Site.css       # Файл стилей
└── js/
    └── Site.js        # Файл JavaScript
```

---

## 📚 Часть 1: HTML (HyperText Markup Language)

### Что такое HTML?

**HTML** — это язык разметки, который определяет структуру веб-страницы. Он состоит из **тегов** (элементов), которые говорят браузеру, как отображать контент.

### Основные теги проекта

#### 1. Каркас документа

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Заголовок страницы</title>
    <link rel="stylesheet" href="css/Site.css">
</head>
<body>
    <!-- Видимое содержимое страницы -->
</body>
</html>
```

| Тег | Назначение |
|-----|------------|
| `<!DOCTYPE html>` | Сообщает браузеру, что это HTML5 |
| `<html>` | Корневой элемент страницы |
| `<head>` | Служебная информация (не видна пользователю) |
| `<body>` | Видимое содержимое страницы |
| `<meta>` | Метаданные (кодировка, масштаб) |
| `<title>` | Заголовок вкладки браузера |
| `<link>` | Подключение внешних файлов (CSS) |

#### 2. Структурные теги

```html
<header>Шапка сайта</header>
<main>Основное содержимое</main>
<footer>Подвал сайта</footer>
<section>Смысловой раздел</section>
<nav>Навигация (меню)</nav>
<div>Блок-контейнер</div>
```

#### 3. Текстовые теги

```html
<h1>Заголовок 1 уровня (самый важный)</h1>
<h2>Заголовок 2 уровня</h2>
<h3>Заголовок 3 уровня</h3>
<p>Абзац текста</p>
<a href="ссылка.html">Текст ссылки</a>
<span>Строчный элемент</span>
```

#### 4. Формы

```html
<form id="login-form">
    <label for="email">Email</label>
    <input type="email" id="email" name="email" placeholder="Введите email">
    
    <label for="password">Пароль</label>
    <input type="password" id="password" name="password">
    
    <button type="submit">Войти</button>
</form>
```

| Атрибут | Назначение |
|---------|------------|
| `type` | Тип поля (email, password, text) |
| `id` | Уникальный идентификатор |
| `name` | Имя поля для отправки на сервер |
| `placeholder` | Подсказка внутри поля |
| `for` | Связывает label с input |

#### 5. Классы и идентификаторы

```html
<!-- Класс - можно использовать много раз -->
<div class="container">
<div class="btn btn-primary">

<!-- Идентификатор - только один раз на странице -->
<nav id="nav-menu">
<form id="login-form">
```

---

## 🎨 Часть 2: CSS (Cascading Style Sheets)

### Что такое CSS?

**CSS** — это язык стилей, который определяет внешний вид HTML-элементов: цвета, шрифты, отступы, расположение.

### Синтаксис CSS

```css
селектор {
    свойство: значение;
}
```

Пример:
```css
body {
    font-family: 'Open Sans', sans-serif;
    margin: 0;
    background-color: #f8f9fa;
}
```

### Способы подключения CSS

```html
<!-- В HTML файле -->
<link rel="stylesheet" href="css/Site.css">
```

### Селекторы

| Селектор | Пример | Описание |
|----------|--------|----------|
| По тегу | `body { }` | Выбирает все теги `<body>` |
| По классу | `.btn { }` | Выбирает все элементы с `class="btn"` |
| По ID | `#nav-menu { }` | Выбирает элемент с `id="nav-menu"` |
| Вложенность | `.header .logo { }` | Элемент `.logo` внутри `.header` |
| Псевдокласс | `a:hover { }` | Ссылка при наведении |

### Основные свойства

#### 1. Шрифты и текст

```css
font-family: 'Open Sans', sans-serif;  /* Шрифт */
font-size: 16px;                       /* Размер шрифта */
font-weight: 700;                      /* Жирность (400 - обычный, 700 - жирный) */
text-align: center;                    /* Выравнивание текста */
line-height: 1.7;                      /* Межстрочный интервал */
color: #333;                           /* Цвет текста */
```

#### 2. Цвета

```css
/* Именованные цвета */
color: white;
background-color: black;

/* HEX-коды */
color: #007bff;      /* Синий */
background-color: #343a40;  /* Тёмный */

/* RGB */
color: rgb(255, 255, 255);
```

#### 3. Отступы и размеры

```css
margin: 0;              /* Внешний отступ */
padding: 20px;          /* Внутренний отступ */
width: 100%;            /* Ширина */
max-width: 1100px;      /* Максимальная ширина */
height: 100vh;          /* Высота (100% от высоты экрана) */
```

**Box Model (блочная модель):**
```
┌─────────────────────────┐
│         margin          │
│  ┌───────────────────┐  │
│  │      border       │  │
│  │  ┌─────────────┐  │  │
│  │  │   padding   │  │  │
│  │  │  ┌───────┐  │  │  │
│  │  │  │content│  │  │  │
│  │  │  └───────┘  │  │  │
│  │  └─────────────┘  │  │
│  └───────────────────┘  │
└─────────────────────────┘
```

#### 4. Flexbox (гибкие контейнеры)

```css
.container {
    display: flex;           /* Включает flex-режим */
    justify-content: center; /* Выравнивание по горизонтали */
    align-items: center;     /* Выравнивание по вертикали */
    flex-wrap: wrap;         /* Перенос элементов */
    gap: 30px;               /* Расстояние между элементами */
}

.item {
    flex: 1;                 /* Растягивание элемента */
    min-width: 250px;        /* Минимальная ширина */
}
```

#### 5. Позиционирование

```css
position: fixed;    /* Фиксированное положение (при прокрутке остаётся) */
position: absolute; /* Абсолютное позиционирование */
position: relative; /* Относительное позиционирование */

top: 0;             /* Положение сверху */
left: 0;            /* Положение слева */
z-index: 1000;      /* Слои (чем больше, тем выше) */
```

#### 6. Медиа-запросы (адаптивность)

```css
/* Для экранов меньше 768px */
@media (max-width: 768px) {
    h1 { font-size: 2.5rem; }
    .menu-toggle { margin-left: auto; }
}
```

---

## ⚙️ Часть 3: JavaScript

### Что такое JavaScript?

**JavaScript** — это язык программирования, который добавляет интерактивность на сайт: реакции на клики, анимации, обработка форм.

### Подключение JavaScript

```html
<!-- В конце body (перед закрывающим тегом) -->
<script src="js/Site.js"></script>
```

### Основы JavaScript в проекте

#### 1. Поиск элементов

```javascript
// По ID
const menuToggle = document.getElementById('mobile-menu');
const navMenu = document.getElementById('nav-menu');

// По классу (возвращает коллекцию)
const links = navMenu.querySelectorAll('a');
```

#### 2. Обработчики событий

```javascript
// Клик по элементу
menuToggle.addEventListener('click', function() {
    // Код выполняется при клике
});

// Перебор элементов
links.forEach(function(link) {
    link.addEventListener('click', function() {
        // Код для каждой ссылки
    });
});
```

#### 3. Работа с классами

```javascript
// Добавить класс
navMenu.classList.add('active');

// Удалить класс
navMenu.classList.remove('active');

// Переключить класс (добавить/удалить)
navMenu.classList.toggle('active');

// Проверить наличие класса
if (navMenu.classList.contains('active')) {
    // Класс есть
}
```

#### 4. Логика мобильного меню

```javascript
// 1. Клик на бургер - открыть/закрыть меню
menuToggle.addEventListener('click', function() {
    navMenu.classList.toggle('active');
    menuToggle.classList.toggle('is-active');
});

// 2. Клик на ссылку - закрыть меню
links.forEach(function(link) {
    link.addEventListener('click', function() {
        navMenu.classList.remove('active');
        menuToggle.classList.remove('is-active');
    });
});

// 3. Клик вне меню - закрыть меню
document.addEventListener('click', function(e) {
    if (!navMenu.contains(e.target) && !menuToggle.contains(e.target)) {
        navMenu.classList.remove('active');
        menuToggle.classList.remove('is-active');
    }
});
```

---

## 🔍 Разбор конкретных элементов проекта

### 1. Шапка сайта (Header)

**HTML:**
```html
<header class="main-header">
    <div class="container">
        <div class="logo">AlR1_Beats</div>
        <div class="menu-toggle" id="mobile-menu">
            <span class="bar"></span>
            <span class="bar"></span>
            <span class="bar"></span>
        </div>
        <nav class="nav-menu" id="nav-menu">
            <ul>
                <li><a href="Site.html">Главная</a></li>
            </ul>
        </nav>
    </div>
</header>
```

**CSS:**
```css
.main-header {
    background-color: #343a40;
    position: fixed;       /* Фиксированная шапка */
    width: 100%;
    top: 0;
    z-index: 1000;         /* Поверх всего */
}

.container {
    max-width: 1100px;
    margin: 0 auto;        /* Центрирование */
    padding: 0 20px;
}
```

### 2. Мобильное меню (Бургер)

**Как работает:**
1. Три полоски (`.bar`) видны всегда
2. При клике добавляется класс `is-active`
3. CSS трансформирует полоски в крестик

**CSS анимация:**
```css
.menu-toggle.is-active .bar:nth-child(1) {
    transform: translateY(8px) rotate(45deg);  /* Поворот на 45° */
}

.menu-toggle.is-active .bar:nth-child(2) {
    opacity: 0;  /* Исчезает */
}

.menu-toggle.is-active .bar:nth-child(3) {
    transform: translateY(-8px) rotate(-45deg);
}
```

### 3. Сетка карточек (Grid/Flex)

**HTML:**
```html
<div class="services-grid">
    <div class="service-card">
        <h3>Заголовок</h3>
        <p>Описание</p>
    </div>
    <!-- Ещё карточки -->
</div>
```

**CSS:**
```css
.services-grid {
    display: flex;
    flex-wrap: wrap;       /* Перенос на новую строку */
    gap: 30px;             /* Расстояние между карточками */
    justify-content: center;
}

.service-card {
    flex: 1;
    min-width: 280px;      /* Минимальная ширина карточки */
    max-width: 350px;
    background-color: #fff;
    padding: 30px;
    border-radius: 10px;
}
```

### 4. Формы входа/регистрации

**HTML:**
```html
<form id="login-form">
    <div class="form-group">
        <label for="email">Email</label>
        <input type="email" id="email" name="email" placeholder="Введите email">
    </div>
    <button type="submit" class="btn btn-primary">Войти</button>
</form>
```

**CSS:**
```css
.form-group {
    margin-bottom: 20px;
}

.form-group label {
    display: block;        /* На отдельной строке */
    margin-bottom: 8px;
}

.form-group input {
    width: 100%;           /* На всю ширину контейнера */
    padding: 12px 15px;
    border: 1px solid #ccc;
    border-radius: 8px;
    box-sizing: border-box;  /* Учитывать padding в ширине */
}
```

---

## 📖 Словарь терминов

| Термин | Значение |
|--------|----------|
| **Тег** | Элемент HTML в угловых скобках `<tag>` |
| **Атрибут** | Дополнительная информация тега (`href`, `class`, `id`) |
| **Селектор** | Правило выбора элементов в CSS |
| **Класс** | Имя для группировки элементов (`.class`) |
| **ID** | Уникальный идентификатор элемента (`#id`) |
| **Flexbox** | Модель гибкой вёрстки |
| **Медиа-запрос** | Правило для разных размеров экрана |
| **Событие** | Действие пользователя (клик, наведение) |
| **Обработчик** | Функция, реагирующая на событие |
| **DOM** | Структура документа в браузере |

---

## 🎯 Задания для практики

### Уровень 1: HTML
1. Добавьте новый пункт меню "Помощь" во все страницы
2. Создайте новую страницу `faq.html` с вопросами и ответами
3. Добавьте изображения в карточки услуг

### Уровень 2: CSS
1. Измените цветовую схему (синий → зелёный)
2. Добавьте hover-эффект для карточек услуг
3. Создайте новый класс `.btn-danger` для красной кнопки

### Уровень 3: JavaScript
1. Добавьте плавный скролл к якорям
2. Сделайте проверку пароля при регистрации
3. Добавьте сообщение при отправке формы

---

## 🔗 Полезные ресурсы

- **HTML:** https://developer.mozilla.org/ru/docs/Web/HTML
- **CSS:** https://developer.mozilla.org/ru/docs/Web/CSS
- **JavaScript:** https://developer.mozilla.org/ru/docs/Web/JavaScript
- **Flexbox:** https://flexboxfroggy.com/ (игра для обучения)
